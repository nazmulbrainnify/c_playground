#include <stdio.h>

int main(){
    int x, y;

    for(x=1;x<=10;x++){
        printf("\n%i Er Namota: \n", x);
        for(y=1; y<=10; y++){
            printf("%i x %i = %i\n", x, y, x*y);
        }
    }

    return 0;
}
